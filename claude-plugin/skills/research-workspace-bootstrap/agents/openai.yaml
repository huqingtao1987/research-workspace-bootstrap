interface:
  display_name: "Research Workspace Bootstrap"
  short_description: "Build a minimal, adaptive research workspace"
  default_prompt: "Use $research-workspace-bootstrap to minimally initialize or audit this research folder while preserving its useful existing structure."

policy:
  allow_implicit_invocation: false
